services:
  irma:
    build: .
    ports:
      - "80:18354"      # Customer accesses http://server (no port needed)
    restart: unless-stopped
    # Uncomment to use external data volume instead of baked-in data:
    # volumes:
    #   - ./data:/app/app/systems/irma/data
